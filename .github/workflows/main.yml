cat <<EOF > .github/workflows/main.yml
name: Action Mes Dates
on: [push, pull_request]

jobs:
  job-ls:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher les fichiers
        run: ls -R
EOF